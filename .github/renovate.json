{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:best-practices"],
  "description": "Regular automation - excludes spritely actions with circular dependencies",
  "packageRules": [
    {
      "description": "Exclude spritely/actions.semantic-version - use manual workflow instead (circular dependency: it uses our devcontainer image)",
      "matchPackageNames": ["spritely/actions.semantic-version"],
      "enabled": false
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Update ARG versions in Dockerfile with renovate comments",
      "managerFilePatterns": [
        "/(^|/)Dockerfile$/",
        "/(^|/)Dockerfile\\.[^/]*$/"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[a-z-]+?) depName=(?<depName>[^\\s]+?)(?: extractVersion=(?<extractVersion>[^\\s]+?))?(?: versioning=(?<versioning>[^\\s]+?))?\\s+ARG [A-Z_]+?=(?<currentValue>.+?)\\s"
      ]
    }
  ]
}
