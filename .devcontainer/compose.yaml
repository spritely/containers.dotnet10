services:
  containers-dotnet10-dev:
    container_name: containers-dotnet10-dev
    build:
      context: ../src
      dockerfile: Containerfile
    command: /bin/sh -c "sleep infinity"
    volumes:
      - ../:/src
      - ${HOME:-$USERPROFILE}/.ssh:/root/.ssh:ro
    env_file:
      - .env
